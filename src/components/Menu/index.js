import React from 'react';
import Logo from '../../assets/img/Logo.png'

function Menu() {
    return (
        <header>
            <img src={Logo} alt='Aluraflix logo' />
        </header>
    );
}

export default Menu;